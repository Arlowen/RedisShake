# RedisShake 极简控制台设计文档

## 1. 背景与问题

当前控制台已覆盖连接管理、任务创建、预检查、运行监控和日志排查，但视觉层级重复：深色大侧栏持续占宽，页面同时出现英文 eyebrow、大标题、长说明、指标条和工具栏；空数据场景仍展示全套统计与筛选；连接表单与任务向导使用多层卡片和重复步骤标题。用户需要先理解页面结构，才能找到真正的主操作。

本设计将控制台调整为克制、清晰的极简运维界面。每个页面只保留一个主标题、一个主操作和完成当前任务所必需的信息。极简不等于删除能力：连接测试、任务预检查、启停、日志筛选、危险操作提醒、Loading、Empty、Error 和响应式状态必须继续保留。

设计参数固定为：`DESIGN_VARIANCE=3`、`MOTION_INTENSITY=2`、`VISUAL_DENSITY=5`。界面强调可扫描性，不使用装饰性渐变、纹理、外发光、大面积深色背景或无功能动效。

## 2. 范围与影响面

| 区域 | 调整范围 |
| --- | --- |
| 全局框架 | 238px 深色侧栏改为 56px 顶栏；品牌、三项导航和服务状态放在同一行 |
| 页面标题 | 移除英文 eyebrow，标题固定为 24px；说明缩短并与主操作同层 |
| 同步任务 | 空数据时隐藏统计和筛选；有数据时使用紧凑摘要、单行筛选和无阴影列表 |
| 连接管理 | 空数据时隐藏工具栏；连接列表减少装饰图形，保留拓扑、凭据和检查时间 |
| 连接抽屉 | 宽度收窄，卡片区块改为分隔线；TLS 和证书仍按需展开 |
| 任务向导 | 移除左侧六步导航和编号方块，改为顶部 `当前步骤 / 总步骤`；字段和流程不变 |
| 任务详情 | 大指标条改为紧凑状态摘要；保留运行概览、日志、历史和配置快照 |
| 系统信息 | 移除状态 Hero 和逐行装饰图标，改为一个状态行和定义列表 |
| 构建部署 | React、Webpack、Jest、Go `embed.FS` 和单端口部署保持不变 |

本次不修改后端 API、SQLite schema、任务状态机、RedisShake worker 管理、凭据加密或数据同步行为。

## 3. 总体方案

控制台采用一个稳定的视觉骨架，页面差异只体现在内容区：

```text
┌ RedisShake ── 同步任务  连接  系统 ───────────── ● Ready ┐
└──────────────────────────────────────────────────────────┘

  页面标题                         [唯一主操作]
  一句话说明
  ────────────────────────────────────────────────────────
  紧凑摘要 / 工具栏（有数据时才显示）
  列表、表单或详情内容
```

信息优先级固定为：

1. 当前页面与主操作。
2. 阻断、警告和运行状态。
3. 完成操作所需的字段或数据。
4. 时间、revision、路径等诊断信息。

组件继续使用 React 和 Ant Design，但通过统一 Token 和 CSS 限制默认视觉重量。Phosphor 图标只用于全局导航、危险提示和空状态，不为每一行数据增加装饰图标。不引入新的第三方 UI 或动画依赖。

## 4. UI 设计

### 4.1 设计 Token

| Token | 值 | 用途 |
| --- | --- | --- |
| 页面背景 | `#f7f8f7` | 中性背景 |
| 主表面 | `#ffffff` | 顶栏、列表、抽屉 |
| 主文字 | `#18201e` | 标题和关键值 |
| 次文字 | `#6f7875` | 说明和元信息 |
| 分隔线 | `#e4e8e6` | 页面与列表分组 |
| 唯一强调色 | `#326b5b` | 主按钮、选中态、成功状态 |
| 危险色 | `#a64d49` | 删除、强制停止和阻断项 |
| 圆角 | `6px / 8px` | 控件和主要容器 |
| 页面最大宽度 | `1180px` | 桌面内容区 |
| 基础间距 | `8px` | 所有间距按 8 的倍数组织 |

字体使用 Geist；数字、ID、路径和日志使用 Geist Mono。页面标题 24px，区块标题 14px，正文 13px，元信息 11px。无标题超过 28px。

### 4.2 全局导航

- 顶栏高度 56px，白色背景，底部 1px 分隔线，不使用阴影。
- 左侧只显示 RedisShake 标识和名称，不显示 `Control plane` 副标题。
- 中间显示三项文本导航；选中态使用强调色文字和 2px 下边线。
- 右侧显示一个静态状态点和 `Ready`，不使用持续呼吸动画。
- 小于 720px 时隐藏品牌文字，导航保持可见并允许横向紧凑排列。

### 4.3 页面标题与工具栏

- 页面标题只包含中文标题、一句不超过 42 个汉字的说明和右侧主操作。
- 每页只允许一个 `primary` 按钮；刷新、复制、下载等为次级或文本按钮。
- 空列表不展示统计、搜索、筛选和排序，只展示空状态和创建按钮。
- 有数据时，摘要使用单行文本，例如 `12 个任务 · 2 个运行中 · 5 个可启动`，不使用四宫格指标。

### 4.4 列表与状态

- 列表使用白色平面和行分隔线，无卡片阴影。
- 行高目标为 56–64px；主字段加粗，元信息放在下一行。
- 状态胶囊高度 20px，浅色背景，仅保留 5px 状态点。
- Loading 使用与列表行等高的骨架；Error 使用一行内联错误；Empty 高度不超过 240px。

### 4.5 表单与抽屉

- 字段标签位于控件上方，字段间距 16px。
- 表单区块不再使用独立卡片，只使用标题、说明和顶部/底部分隔线。
- 连接抽屉桌面宽度 560px；任务向导桌面宽度 760px。
- 抽屉底部固定展示取消和唯一主操作。
- TLS 证书、过滤字段和高级参数继续按当前开关展开，默认不增加视觉噪声。
- 危险操作保留浅红或浅琥珀背景，不能为了极简而弱化风险。

### 4.6 任务向导

- 顶部显示 `步骤 2 / 6`、当前步骤名称和一条 2px 进度线。
- 正文只显示当前步骤标题、短说明和字段，不再重复左侧步骤列表或编号方块。
- 上一步、稍后继续、下一步/预检查/启动固定在底部。
- 自动保存状态以 11px 文本显示；保存失败必须使用危险色。

### 4.7 任务详情与系统信息

- 任务详情在标题下显示一行状态摘要：任务状态、Run 状态、累计读写、OPS、心跳。
- Tabs 仍是查看运行概览、日志、历史和配置的唯一二级导航。
- 日志工具保持搜索、级别、暂停和下载，但使用紧凑控件。
- 系统页只展示 Ready 状态和六行运行配置，不再使用 Hero、图标矩阵或重复状态卡片。

### 4.8 响应式与交互状态

- 390px 宽度下不出现横向滚动。
- 顶栏、标题、工具栏和表单在 720px 下切换为单列；表格行隐藏次要列但保留主操作。
- 动效只允许 `opacity` 和 `transform`，时长 120–180ms；按钮按下使用 `translateY(1px)`。
- 尊重 `prefers-reduced-motion`，关闭页面入场、骨架以外的非必要动效。

## 5. 核心模型与接口契约

本次不修改 Connection、Task、Run、SystemInfo、日志或预检查的模型和接口。前端继续使用现有 `/api/v1` 契约、SSE Run 状态事件和日志分页接口。

`PageHeader` 组件契约调整为：

```ts
type PageHeaderProps = {
  title: string
  description: string
  children?: ReactNode
}
```

不再接受或渲染 eyebrow。`SystemInfo.web_ui_configured` 继续用于证明 Web 资源已内嵌到控制面。

## 6. 关键流程

### 6.1 创建任务

用户在空状态或任务页标题区点击唯一主按钮，进入精简向导。每步仍执行原有字段校验和草稿自动保存；进入最后一步时运行预检查。阻断项禁止启动，危险警告必须确认。

### 6.2 管理连接

用户从连接页创建或编辑连接。基础连接默认展开；Sentinel 和 TLS 只在选择对应能力后出现。测试结果仍按通过、警告和阻断展示，目标写检查的临时 Key 副作用必须保持可见。

### 6.3 监控运行

用户进入任务详情后先看到状态摘要，再按需进入日志或诊断 Tabs。停止和强制停止仍要求确认；UNKNOWN 仍显示不可误操作的阻断说明。

## 7. 兼容、迁移与降级

此次为纯前端呈现变更，不涉及数据迁移。现有 SQLite、连接凭据、任务、运行历史和 URL 路由保持兼容；深链仍由 Go SPA fallback 提供。

如果 React 资源未嵌入，后端继续以 API-only 模式运行；`REDISSHAKE_WEB_DIR` 仍可覆盖内嵌资源。回滚只需切回上一提交并重新执行 `backend/build_web.sh`，无需回滚数据库。

## 8. 验收标准与验证方式

- 仓库根目录存在 `design.md`，实现中的 Token、布局和响应式规则与文档一致。
- 桌面端不再显示深色侧栏、英文 eyebrow、四宫格指标和多层表单卡片。
- 空任务/空连接页面不显示无意义的统计、筛选和刷新工具栏。
- 每个页面最多一个 primary 主操作，危险操作除外。
- 任务向导只保留一个顶部进度指示，不再显示左侧 Steps 和编号方块。
- 连接、任务、预检查、启停、日志和系统信息能力没有丢失。
- Loading、Empty、Error、危险警告、UNKNOWN 和运行中状态仍可辨识。
- 1440px、1024px、720px 和 390px 均无横向溢出；390px 主操作可点击。
- `npm ci`、TypeScript、Jest、Webpack、npm audit、Go test/vet/race、文档构建和 Dockerfile 构建通过。
- 真实浏览器完成连接抽屉、任务向导、三项导航、深链和单端口 `embed.FS` 验证；无 page error 或 console error。
- 变更提交并推送后，远程 CI 的控制面、Web E2E、Redis/Valkey 和模块矩阵全部通过。
