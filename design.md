# RedisShake 官网统一极简控制台设计文档

## 1. 背景与问题

控制台已完成极简化并覆盖连接管理、任务创建、预检查、运行监控和日志排查，但仍使用自定义绿色品牌、过于细碎的字号、56px 导航和边框型列表，与 RedisShake 官网形成两套视觉体系。用户从官网进入控制台时不应感知为另一个产品。

本设计以 [RedisShake 官网](https://tair-opensource.github.io/RedisShake/) 2026-08-26 的实时 VitePress 页面和 CSS 为品牌基线，在不恢复复杂层级的前提下统一颜色、字体、顶栏、按钮、表面、圆角、移动导航和明暗模式。极简不等于删除能力：连接测试、任务预检查、启停、日志筛选、危险操作提醒、Loading、Empty、Error 和响应式状态必须继续保留。

设计参数固定为：`DESIGN_VARIANCE=3`、`MOTION_INTENSITY=2`、`VISUAL_DENSITY=5`。界面强调可扫描性，不使用装饰性渐变、纹理、外发光、大面积深色背景或无功能动效。

## 2. 范围与影响面

| 区域 | 调整范围 |
| --- | --- |
| 全局框架 | 保留官网 64px 品牌顶栏；业务导航改为官网文档页同类左侧 Sidebar，移动端由汉堡按钮打开 |
| 品牌体系 | 自定义绿色/Geist 改为官网蓝色、Inter/system-ui、官网浅灰表面和 12px 圆角 |
| 明暗模式 | 复用官网 light/dark Token；顶栏提供浅色、深色、跟随系统三种选择并记忆用户偏好 |
| 页面标题 | 保留单层中文标题和说明，标题大小对齐官网文档内容层级 |
| 同步任务 | 搜索、状态筛选和排序工具栏始终可见；使用带表头的紧凑数据列表 |
| 连接管理 | 搜索始终可见；使用带表头的紧凑数据列表；Web 控制台只创建和编辑单机连接 |
| 连接编辑 | 创建和编辑共用简化页面，只填写名称、Redis 地址、用户名和密码 |
| 任务编辑 | 删除六步向导与自动跳步，改为独立路由的单页任务表单；保存草稿、预检查和启动在同一操作区 |
| 任务详情 | 大指标条改为紧凑状态摘要；保留运行概览、日志、历史和配置快照 |
| 构建部署 | React、Webpack、Jest、Go `embed.FS` 和单端口部署保持不变 |

除下线 SystemInfo 查询接口外，本次不修改其他后端 API、SQLite schema、任务状态机、RedisShake worker 管理、凭据加密或数据同步行为。

## 3. 总体方案

控制台采用一个稳定的视觉骨架，页面差异只体现在内容区：

```text
┌ RedisShake ─────────────────────────── ◐  ● Ready ┐
└──────────────────────────────────────────────────────────┘
┌──────────────┬───────────────────────────────────────────┐
│ 同步任务     │ 页面标题                    [唯一主操作] │
│ 连接管理     │ 固定搜索 / 筛选工具栏                  │
│              │ 列表、表单或详情内容                    │
└──────────────┴───────────────────────────────────────────┘
```

信息优先级固定为：

1. 当前页面与主操作。
2. 阻断、警告和运行状态。
3. 完成操作所需的字段或数据。
4. 时间、revision、路径等诊断信息。

组件继续使用 React 和 Ant Design，但由官网实测 Token 驱动 Ant Design theme 和 CSS。Phosphor 图标只用于主题切换、移动 Sidebar、危险提示和空状态，不为 Sidebar 或每一行数据增加装饰图标。不引入新的第三方 UI 或动画依赖。

## 4. UI 设计

### 4.1 设计 Token

| Token | 值 | 用途 |
| --- | --- | --- |
| Light 页面背景 | `#ffffff` | 官网 body 背景 |
| Light 软表面 | `#f6f6f7` | 官网 Feature 卡片、列表和空状态 |
| Light 次按钮 | `#ebebef` | 官网 alt button |
| Light 主文字 | `#3c3c43` | 官网正文和标题 |
| Light 次文字 | `rgba(60,60,67,.78)` | 官网 tagline 和说明 |
| Light 品牌文字 | `#3451b2` | 官网 Hero 品牌色、选中态 |
| Light 主按钮 | `#5672cd` | 官网 brand button |
| Dark 页面背景 | `#1b1b1f` | 官网暗色 body |
| Dark 软表面 | `#202127` | 官网暗色 Feature 卡片 |
| Dark 次按钮 | `#32363f` | 官网暗色 alt button |
| Dark 主文字 | `rgba(255,255,245,.86)` | 官网暗色正文 |
| Dark 品牌文字 | `#a8b1ff` | 官网暗色 Hero 品牌色 |
| Dark 主按钮 | `#3e63dd` | 官网暗色 brand button |
| 危险色 | `#a64d49` | 删除、强制停止和阻断项 |
| 圆角 | `12px / 20px` | 官网 Feature 表面 / 按钮 |
| 页面最大宽度 | `1152px` | 官网首页 Feature 内容宽度 |
| 基础间距 | `8px` | 所有间距按 8 的倍数组织 |

字体采用 ChatGPT、Claude、DeepSeek 同类产品常见的原生 UI 策略：`ui-sans-serif, -apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang SC", "Microsoft YaHei", sans-serif`；数字、ID、路径和日志使用系统等宽字体。全局只保留 12px 元信息、13px 控件、14px 正文、16–18px 区块标题和 26px 页面标题五档字号，不再使用 8–11px 正文。控制台不复刻官网 56px Hero，而采用官网文档内容层级。

### 4.2 顶栏与 Sidebar

- 顶栏高度 64px，使用官网页面背景与分隔线，不使用阴影。
- 左侧与官网一致，仅显示 `RedisShake` 文本品牌，不增加独立 Logo 容器。
- 顶栏右侧只显示主题菜单和 Ready 状态；菜单提供跟随系统、浅色模式和深色模式，跟随系统时实时响应 `prefers-color-scheme` 变化。
- 桌面左侧 Sidebar 宽度 240px，不显示分组标题，直接依次展示同步任务和连接管理。
- Sidebar 选中项使用官网品牌文字色与浅灰背景，不使用图标、渐变或阴影。
- 小于 900px 时 Sidebar 移出视口，顶栏显示汉堡按钮；展开后使用遮罩，点击链接、遮罩或 Escape 均关闭。

### 4.3 页面标题与工具栏

- 页面标题只包含中文标题、一句不超过 42 个汉字的说明和右侧主操作。
- 每页只允许一个 `primary` 按钮；刷新、复制、下载等为次级或文本按钮。
- 任务和连接列表始终展示搜索工具栏；任务提供排序，刷新操作位于工具栏右侧。
- 空列表不展示大面积占位卡片，只在表格区域显示一行紧凑的暂无数据提示；创建按钮固定在标题区。
- 有数据时，摘要使用单行文本，例如 `12 个任务 · 2 个运行中 · 5 个可启动`，不使用四宫格指标。

### 4.4 列表与状态

- 列表使用官网 `#f6f6f7` 软表面、12px 圆角、明确表头和轻分隔线，无卡片阴影。
- 行高目标为 56–64px；主字段加粗，元信息放在下一行。
- 状态胶囊高度 20px，品牌色使用官网蓝色，危险与警告仍保留语义色。
- Loading 使用与列表行等高的骨架；Error 使用一行内联错误；Empty 高度不超过 240px。

### 4.5 表单、页面与抽屉

- 字段标签位于控件上方，字段间距 16px。
- 表单区块不再使用独立卡片，只使用标题、说明和顶部/底部分隔线。
- 新建连接使用 `/connections/new` 独立页面，现有连接继续使用 560px 抽屉编辑；任务创建与编辑使用独立页面。
- 任务表单内容宽度不超过 960px，标题区展示取消、保存草稿和唯一主操作。
- 主按钮使用官网 40px 胶囊样式；次按钮使用官网浅灰 alt button 样式。
- Web 连接表单暂不展示 Sentinel、Cluster、TLS 或证书配置；任务过滤字段和高级参数继续按当前开关展开。
- 危险操作保留浅红或浅琥珀背景，不能为了极简而弱化风险。

### 4.6 独立任务编辑页

- 不显示 Steps、步骤编号、上一步或下一步。
- `/tasks/new` 创建页和 `/tasks/:id/edit` 编辑页依次展示基本信息、同步链路、同步范围和可折叠高级设置。
- 页面标题区展示取消、保存草稿，以及执行预检查或启动任务的唯一主操作。
- 保存草稿只要求任务名称；预检查要求源端、目标端完整且不能相同。
- 表单修改后已通过的预检查立即失效，必须重新保存和预检查后才能启动。

### 4.7 任务详情

- 任务详情在标题下显示一行状态摘要：任务状态、Run 状态、累计读写、OPS、心跳。
- Tabs 仍是查看运行概览、日志、历史和配置的唯一二级导航。
- 日志工具保持搜索、级别、暂停和下载，但使用紧凑控件。

### 4.8 响应式与交互状态

- 390px 宽度下不出现横向滚动。
- Sidebar 在 900px 下切换为汉堡抽屉；标题、工具栏和表单切换为单列；表格行隐藏次要列但保留主操作。
- 动效只允许 `opacity` 和 `transform`，时长 120–180ms；按钮按下使用 `translateY(1px)`。
- 尊重 `prefers-reduced-motion`，关闭页面入场、骨架以外的非必要动效。
- Light/Dark 切换后页面、抽屉、Modal、输入框、列表、日志和状态均满足对比度，不出现硬编码白色断层。

## 5. 核心模型与接口契约

SystemInfo 页面、前端模型和 `/api/v1/system/info` 接口已下线。Connection、Task、Run、日志和预检查继续使用现有 `/api/v1` 契约、SSE Run 状态事件和日志分页接口。

`PageHeader` 组件契约调整为：

```ts
type PageHeaderProps = {
  title: string
  description: string
  children?: ReactNode
}
```

不再接受或渲染 eyebrow。

主题偏好仅保存在浏览器：`localStorage['redisshake-theme']` 的值为 `light`、`dark` 或 `system`；首次访问默认为 `system`。选择 `system` 后监听 `prefers-color-scheme` 的 `change` 事件并实时解析为浅色或深色。主题不进入后端模型或 API。

## 6. 关键流程

### 6.1 创建任务

用户在任务页标题区点击唯一主按钮，进入 `/tasks/new` 独立创建页。用户可以只保存草稿，也可以填完源端、目标端与范围后直接预检查；首次预检查创建草稿后 URL 切换到可恢复的 `/tasks/:id/edit`。预检查通过后同一位置切换为“启动任务”。阻断项禁止启动，危险警告必须确认。

### 6.2 管理连接

用户从连接页进入 `/connections/new` 独立页面创建连接，编辑现有连接复用同一简化页面。Web 控制台固定提交单机拓扑，只采集名称、地址、用户名和密码。测试结果仍按通过、警告和阻断展示，目标写检查的临时 Key 副作用必须保持可见。

### 6.3 监控运行

用户进入任务详情后先看到状态摘要，再按需进入日志或诊断 Tabs。停止和强制停止仍要求确认；UNKNOWN 仍显示不可误操作的阻断说明。

## 7. 兼容、迁移与降级

此次变更不涉及数据迁移。现有 SQLite、连接凭据、任务和运行历史保持兼容；`/system` 不再作为业务路由，其余深链仍由 Go SPA fallback 提供。

如果 React 资源未嵌入，后端继续以 API-only 模式运行；`REDISSHAKE_WEB_DIR` 仍可覆盖内嵌资源。回滚只需切回上一提交并重新执行 `backend/build_web.sh`，无需回滚数据库。

## 8. 验收标准与验证方式

- 仓库根目录存在 `design.md`，实现中的 Token、布局和响应式规则与文档一致。
- Light 模式的 `#3451b2/#5672cd/#f6f6f7/#3c3c43`、Dark 模式的 `#1b1b1f/#202127/#a8b1ff/#3e63dd` 与官网实时 CSS 一致。
- 顶栏高度、纯文本品牌、左侧文档式 Sidebar、移动汉堡菜单、40px 胶囊按钮和 12px 软表面与官网视觉语言一致。
- 浅色、深色和跟随系统三种模式可切换、可持久化；跟随系统时 OS 外观变化可实时生效，并覆盖 Ant Design Portal 中的抽屉和 Modal。
- 桌面端不再显示深色侧栏、英文 eyebrow、四宫格指标和多层表单卡片。
- 任务和连接页面始终显示搜索工具栏及对应筛选；空数据时不显示大面积占位卡片，只显示紧凑空行。
- 任务与连接列表有明确表头；任务搜索支持名称，连接搜索同时支持名称和地址；筛选、排序、刷新和创建入口在空数据时仍可操作。
- 每个页面最多一个 primary 主操作，危险操作除外。
- 代码和页面中不存在 TaskWizard、Steps、步骤编号、上一步或下一步；创建与编辑均使用独立路由的单页 TaskEditorPage。
- 新建和编辑连接均使用简化的单机连接页面，操作按钮位于表单底部。
- 同步任务和连接管理只出现在左侧 Sidebar；桌面顶栏不再承载业务导航。
- 连接、任务、预检查、启停和日志能力没有丢失。
- Loading、Empty、Error、危险警告、UNKNOWN 和运行中状态仍可辨识。
- 1440px、1024px、720px 和 390px 均无横向溢出；390px 主操作可点击。
- `npm ci`、TypeScript、Jest、Webpack、npm audit、Go test/vet/race、文档构建和 Dockerfile 构建通过。
- 真实浏览器完成简化连接页、独立任务创建页、两项 Sidebar 导航、深链和单端口 `embed.FS` 验证；无 page error 或 console error。
- 变更提交并推送后，远程 CI 的控制面、Web E2E、Redis/Valkey 和模块矩阵全部通过。
