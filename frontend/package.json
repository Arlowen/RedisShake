{
  "name": "redisshake-web-console",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "npm run typecheck && node scripts/build.mjs",
    "typecheck": "node --check src/lib.js && node --check src/components.js && node --check src/pages.js && node --check src/app.js && node --check scripts/build.mjs && node --check scripts/dev.mjs",
    "test": "node --test test/*.test.js",
    "test:e2e": "playwright test"
  },
  "devDependencies": { "@playwright/test": "1.62.1" }
}
